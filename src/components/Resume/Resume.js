import resume from '../../images/resume-ss.png'

const Resume = () => {
  return (
    <div className='resume-wrapper'>
      <img src={resume} className="contact-icons" name="linked in" alt="linked in link" />
    </div>
  )
}

export default Resume